[tool.ruff]
target-version = "py311"
line-length = 100
src = ["scraper"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
known-first-party = ["catalog_io", "parser", "pricing_diff"]
